program tikal;

uses
  System.StartUpCopy,
  FMX.Forms,
  TabbedTemplate in 'TabbedTemplate.pas' {uAna},
  uDetail in 'uDetail.pas' {frm_Detail: TFrame};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TuAna, uAna);
  Application.Run;
end.
